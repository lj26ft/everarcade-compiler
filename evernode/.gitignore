# Evernode deployment runtime artifacts are generated by bootstrap,
# validation, certification, or future lease/package flows.
*

# Keep the source-controlled layout and Phase 1 manifest visible.
!.gitignore
!*/
!.gitkeep
!lease/deployment-manifest.txt
