[package]
name = "execution-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1.3.3"
sha2 = "0.10"
hex = "0.4"

contract-api = { path = "../contract-api" }
set-contract = { path = "../contracts/set-contract" }
increment-contract = { path = "../contracts/increment-contract" }
