# UI Renderer Projection

This directory contains deterministic, non-authoritative renderer projection artifacts for the ui domain. The renderer reads canonical projection data only and never mutates protocol state.
