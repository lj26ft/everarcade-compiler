# Projection Model

`projection_model.sh` defines Renderer Runtime v0.1 canonical projection artifacts and root generation. It is intentionally data-oriented and deterministic so validation, certification, and replay equivalence can run without renderer authority.
