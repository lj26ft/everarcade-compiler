# REPLAY Renderer Projection

This directory contains deterministic, non-authoritative renderer projection artifacts for the replay domain. The renderer reads canonical projection data only and never mutates protocol state.
