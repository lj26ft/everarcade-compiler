[package]
name = "renderer-client"
version = "0.1.0"
edition = "2021"

[dependencies]
execution-core = { path = "../../execution-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
