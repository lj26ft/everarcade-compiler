# Sovereign Runtime Client (Local)

Provides local runtime shell requirements:
- deterministic replay and snapshot restore
- event streaming and runtime metrics
- GPU boundary hook points for non-authoritative workloads
