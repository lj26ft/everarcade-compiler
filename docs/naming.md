# Naming
- Sovereign VM = base deterministic execution kernel
- EverArcade = product/ecosystem for games, worlds, economies
- everarcade-host = Linux operator/node runtime
- XRPL = settlement/root anchoring layer
- Evernode = hosted execution/deployment layer
- IPFS = artifact publication layer
