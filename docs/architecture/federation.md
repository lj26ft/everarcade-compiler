# Federation

Federation lets multiple infrastructure participants coordinate world execution, synchronization, failover, and recovery.

It is an operator-facing model for keeping worlds available without making a single machine the permanent center of authority.

See [federation runtime](./federation/federation-runtime), [synchronization](./federation/synchronization), and [recovery](./federation/recovery).
