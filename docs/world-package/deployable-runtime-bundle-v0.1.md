# Deployable Runtime Bundle v0.1

The runtime bundle is deployable infrastructure packaging. It is built from a verified `world.evr` plus a target lease and includes the adapter entrypoint, transport bridge metadata, manifests, contract WASM or stub, genesis state, and verification schemas.

Unlike the world package, a deployable bundle is lease-specific.
