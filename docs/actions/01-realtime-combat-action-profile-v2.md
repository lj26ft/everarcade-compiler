# Realtime combat action profile V2

The compiler emits the nine combat actions with origin class, successor execution tuple, reducer binding, payload limit, receipt type, and derivation/write limits. World Factory rebuilds of Continuum Runner: Arena are byte-identical and packages missing any required action fail combat conformance.
