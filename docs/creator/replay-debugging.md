# Replay Debugging

The replay visualizer reconstructs timelines, windows, checkpoints, and divergence views. It is reconstruction-only and rejects direct replay mutation.

Use timeline inspection to compare continuity roots, window inspection to isolate ranges, and divergence visualization to identify the first divergent frame.
