# Runtime Capability Requirements v1

`runtime/capability-requirements.json` declares PTW runtime version, primitive capabilities, action-handler capabilities, invariant capabilities, proof capabilities, numeric and coordinate models, checkpoint, journal, and root capabilities. Authority implementations compare this manifest with their supported capability manifest before execution.
