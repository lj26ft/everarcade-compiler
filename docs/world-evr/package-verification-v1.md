# Package Verification v1

Verification is independent of a live Authority. It recomputes package bytes evidence, manifest integrity, Runtime Hash, World Hash, Package Hash, component hashes, Runtime IR conformance, references, deterministic initial state, explicit capabilities, and unsupported mandatory features.
