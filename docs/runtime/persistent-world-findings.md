# Persistent World Findings

Measured from deterministic simulation scripts in `world/reports/`.

- Replay growth tracks linearly with tick count under fixed operation density.
- Checkpoint frequency reduces replay restore depth but increases archive growth.
- Scheduler pressure remains stable due to deterministic queue ordering.
- Archive growth is predictable and append-only.
- Restoration limitations: partial restore depends on checkpoint granularity.

Measured sections are generated by `scripts/run_world_simulation.sh` and `scripts/generate_performance_report.sh`; inferred conclusions are explicitly marked in generated reports.
