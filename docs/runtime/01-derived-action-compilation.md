# Derived-action compilation

Successor packages must declare the complete tuple: Execution Envelope V2, Journal V3, Receipt Bundle V2, derived actions enabled, and bounded limits. The P5C base certificate fixture-validates this tuple. Deterministic compiler emission is reserved for the stronger production-model verdict.
