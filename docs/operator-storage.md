# Operator Storage

Protocol truth is in canonical artifacts (receipts/checkpoints/anchors). Indexes and reports are derived and rebuildable.

Use:

- `everarcade-host status --state <path> --storage`
