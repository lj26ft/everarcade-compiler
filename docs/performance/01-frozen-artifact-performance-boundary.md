# Frozen artifact performance boundary

The compiler continues producing byte-identical State V10 Runtime IR and `world.evr`. Frontier-first access, packed content, caching, process scheduling, and timing evidence are Authority implementation details and are excluded from compiler artifacts and protocol roots.

P1 does not alter compiler inputs, canonical encodings, action inventory, receipt/action mapping, or any pinned commitment.
