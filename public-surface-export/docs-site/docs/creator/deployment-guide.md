# Creator Deployment Guide

The creator dashboard manages projects, package status, replay health, and deterministic deployment lineage. Deployments reference content package hashes and registry continuity roots.

Invalid package mutation is rejected. Deployment state is inspectable but does not bypass runtime authority.
