# Distributed Execution Persistence

Execution continuity persists to `.everarcade/distributed_receipts/` with receipts, index, and manifest.

Persist path guarantees atomic write, manifest update, and decode round-trip verification.
