# Replay Verification

Replay verification answers a simple public question: can someone independently verify a world's history?

Operators retain the inputs, receipts, and checkpoints required to replay a history window and compare the result with the published record.

See [runtime replay verification](../runtime/replay_verification) and [receipt canonicalization](../runtime/receipt_canonicalization) for implementation details.
