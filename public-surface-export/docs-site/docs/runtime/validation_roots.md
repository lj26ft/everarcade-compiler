# Validation Roots
`RuntimeValidationRoot` is the canonical hash of runtime metrics. `RuntimeEquivalenceRoot` and `RuntimeContinuityRoot` define stable surfaces for replay/federation proof packaging.
