# Runtime Validation
Canonical runtime validation pipeline is executed by `scripts/run_full_runtime_validation.sh` and enforces deterministic stage ordering for vendor, execution, replay, isolation, epoch, restoration, partition, witness, scheduler, continuity, and snapshot surfaces.
