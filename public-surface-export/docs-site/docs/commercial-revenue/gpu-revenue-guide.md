# GPU Revenue Guide

GPU revenue represents GPU job revenue, artifact revenue, verification revenue, and capacity revenue. The model references the GPU marketplace in `gpu/marketplace` as an integration source while keeping this layer deterministic and local.

GPU provider beneficiaries receive distribution intents, not live payments. Capacity and verification records are replayable economic facts for commercial launch preparation.
