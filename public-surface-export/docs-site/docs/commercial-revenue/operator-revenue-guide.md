# Operator Revenue Guide

Operator revenue represents lease revenue, hosting revenue, federation revenue, and runtime revenue. Events reference operator origins such as lease records, federation topology, and runtime hosting records.

Operator records are inputs to distribution intents and unsigned settlement intents. The layer does not perform operator billing or live settlement execution.
