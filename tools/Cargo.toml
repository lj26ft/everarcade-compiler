[package]
name = "tools"
version = "0.1.0"
edition = "2021"

[lib]
name = "tools"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
content-registry = { path = "../runtime/content-registry" }
