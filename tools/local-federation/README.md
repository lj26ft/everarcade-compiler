# Local Federation Sandbox

Provides developer-local federation lifecycle APIs:

- `start_local_federation()`
- `replay_local_world()`
- `inspect_local_continuity()`

Boot topology:
- Node A
- Node B
- Node C
