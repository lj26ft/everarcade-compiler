[package]
name = "provider-evernode"
version = "0.1.0"
edition = "2021"

[lib]
name = "provider_evernode"
path = "src/lib.rs"

[dependencies]
control-plane = { path = "../control-plane" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
