# XRPL Receipts

Receipt artifacts model XRPL settlement evidence: transaction hash, ledger index, account, destination, amount, outcome, timestamp, transaction root, and intent root. Imported evidence validates receipt, transaction, and intent matching.
