# Settlement Intent

Deterministic intent records capture intent id, asset, amount, sender, recipient, timestamp, and runtime reference. The intent root is the SHA-256 hash of the canonical field order.
