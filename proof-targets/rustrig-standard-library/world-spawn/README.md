# Proof Target: world.spawn

Maturity: CANDIDATE

This target defines the verifier handoff for `world.spawn`. It describes state, input, validation, transition, receipt, replay, and root behavior without claiming formal proof for the full standard library.
