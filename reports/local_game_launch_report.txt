Local Game Launch Report
Date: 2026-06-09
Template selected: creator-sdk/templates/arena
Final classification: Network Transport & Session Synchronization Proven

Requested flow and result

| Step | Result | Evidence |
| --- | --- | --- |
| create | PASS | `node creator-sdk/cli/everarcade.mjs new --template arena --name audit-arena --dir /tmp/everarcade-local-launch-proof/audit-arena` creates the arena project. |
| build | PASS | `node creator-sdk/cli/everarcade.mjs build --project /tmp/everarcade-local-launch-proof/audit-arena` writes Creator SDK build metadata. |
| test | PASS | `node creator-sdk/cli/everarcade.mjs test --project /tmp/everarcade-local-launch-proof/audit-arena` validates required template fields and entry file. |
| package | PASS | `node creator-sdk/cli/everarcade.mjs package --project /tmp/everarcade-local-launch-proof/audit-arena` writes `dist/runtime-package/manifest.json`, `world.wasm`, and `world.json`. |
| start runtime | PASS | `node creator-sdk/cli/everarcade.mjs launch-local --project /tmp/everarcade-local-launch-proof/audit-arena --runtime-root /tmp/everarcade-local-launch-proof/runtime-root` starts `everarcade-runtime` against the generated package. |
| produce session record | PASS | Runtime writes `runtime-root/worlds/audit-arena-world/sessions/session-0001.json`. |
| produce projection record | PASS | Runtime writes `runtime-root/worlds/audit-arena-world/projections/projection-0001.json` with `non_authoritative_projection: true`. |
| network transport session proof | PASS | `node creator-sdk/cli/everarcade.mjs play-network-local --template arena --project /tmp/everarcade-local-launch-proof/audit-arena --runtime-root /tmp/everarcade-local-launch-proof/runtime-root` starts local Client A and Client B identities, routes join/move/attack intents through deterministic transport messages, keeps the runtime authoritative, synchronizes client/runtime state roots, propagates receipts, observes the journal, and verifies replay. |

Honest playability finding

The Arena template now proves a local network-style transport and synchronization session. The Creator SDK can package the Arena template, start local Client A and Client B identities, route joins, movement, and deterministic attack actions through a canonical transport layer, keep the runtime authoritative for state mutation, receipts, checkpoints, journals, and replay roots, synchronize both client observations to the runtime state root, and verify replay to the final session root. The runtime still does not prove federation, Evernode deployment, WAN networking, multi-lease coordination, renderer-driven gameplay, public testnet operation, or production hosting.

Final classification rationale

- `Network Transport & Session Synchronization Proven`: a local Arena session synchronizes Client A and Client B through deterministic transport messages, authoritative runtime mutation, receipt delivery, journal observation, multi-tick progression, and replay verification.
- Not `Federation Proven`, not `Evernode Proven`, and not `Production Multiplayer Proven`: no federation, Evernode lease deployment, WAN transport, multi-lease coordination, settlement, public testnet, or production hosting flow was proven.
- Therefore the end-to-end local game launch classification is **Network Transport & Session Synchronization Proven**.
