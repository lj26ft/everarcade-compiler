Local Game Launch Report
Date: 2026-06-09
Template selected: creator-sdk/templates/arena
Final classification: Multiplayer Local Session Proven

Requested flow and result

| Step | Result | Evidence |
| --- | --- | --- |
| create | PASS | `node creator-sdk/cli/everarcade.mjs new --template arena --name audit-arena --dir /tmp/everarcade-local-launch-proof/audit-arena` creates the arena project. |
| build | PASS | `node creator-sdk/cli/everarcade.mjs build --project /tmp/everarcade-local-launch-proof/audit-arena` writes Creator SDK build metadata. |
| test | PASS | `node creator-sdk/cli/everarcade.mjs test --project /tmp/everarcade-local-launch-proof/audit-arena` validates required template fields and entry file. |
| package | PASS | `node creator-sdk/cli/everarcade.mjs package --project /tmp/everarcade-local-launch-proof/audit-arena` writes `dist/runtime-package/manifest.json`, `world.wasm`, and `world.json`. |
| start runtime | PASS | `node creator-sdk/cli/everarcade.mjs launch-local --project /tmp/everarcade-local-launch-proof/audit-arena --runtime-root /tmp/everarcade-local-launch-proof/runtime-root` starts `everarcade-runtime` against the generated package. |
| produce session record | PASS | Runtime writes `runtime-root/worlds/audit-arena-world/sessions/session-0001.json`. |
| produce projection record | PASS | Runtime writes `runtime-root/worlds/audit-arena-world/projections/projection-0001.json` with `non_authoritative_projection: true`. |
| multiplayer local session proof | PASS | `node creator-sdk/cli/everarcade.mjs play-local-multiplayer --template arena --project /tmp/everarcade-local-launch-proof/audit-arena --runtime-root /tmp/everarcade-local-launch-proof/runtime-root` keeps an Arena session alive across two joins, independent moves, attack, score update, and replay ticks, mutates state, emits receipts, journal, transcript, and replay proof evidence, and verifies replay. |

Honest playability finding

The Arena template now proves a local same-session multiplayer EverArcade game session. The Creator SDK can package the Arena template, launch a persistent local session, join Player A and Player B, process independent movement and deterministic attack actions, update score over multiple ticks, emit receipts and journal evidence for both players, write a multiplayer session transcript, and verify replay to the final session root. The runtime still does not prove networked multiplayer, renderer-driven gameplay, public testnet operation, or production hosting.

Final classification rationale

- `Multiplayer Local Session Proven`: a local Arena session persists across two player joins, independent moves, deterministic attack, score update, and replay verification ticks while state evolves and receipts, journal, transcript, and replay proof are produced.
- Not `Network Multiplayer Proven`: no network transport, federation, renderer-driven gameplay, settlement, public testnet, or production hosting flow was proven.
- Therefore the end-to-end local game launch classification is **Multiplayer Local Session Proven**.
