# JOBS GPU Runtime Domain

This directory contains deterministic GPU Runtime v0.1 descriptors for the `jobs` domain. The domain is non-authoritative, replay-safe, and derives roots from canonical projection inputs only.
