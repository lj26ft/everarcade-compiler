# quests Rustrigs

Canonical implementation lives in `rustrigs/src/quests/mod.rs` so the workspace member can expose the deterministic primitive through Rust's crate module system while retaining this milestone directory surface.
