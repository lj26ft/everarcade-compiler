# dialogue Rustrigs

Canonical implementation lives in `rustrigs/src/dialogue/mod.rs` so the workspace member can expose the deterministic primitive through Rust's crate module system while retaining this milestone directory surface.
