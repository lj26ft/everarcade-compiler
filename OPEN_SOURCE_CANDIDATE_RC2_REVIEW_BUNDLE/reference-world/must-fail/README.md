# Must-Fail Reference World

Any reference world whose signing key differs from `TRUST_ROOT.md`, or whose payload bytes differ from the signed/hash-manifest roots, must fail package or attestation verification.
